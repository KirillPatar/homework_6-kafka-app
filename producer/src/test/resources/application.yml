spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: testGroup
    producer:
      bootstrap-servers: localhost:9092
    bootstrap-servers: localhost:9092
test:
  topic: test_topic_no_partitions